% layout 'default';
<nav class="<%= $nav_style %>">
	<header>
		<%== $header %>
	</header>
	<menu>
		<%== $sidebar %>
		% if ($c->get_req_user eq 'mayor' && ! $c->session('username')) {
		<ul>
			<li><label for="drawer-toggle">Join/Login</label>
		</ul>
		% }
	</menu>
	<footer>
		<%== $footer %>
	</footer>
</nav>
<main>
	<%== $content %>
</main>
