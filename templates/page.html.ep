% layout 'default';
<div id="sidebar">
	<header>
		<%== $header %>
	</header>
	<nav>
		<%== $sidebar %>
		% if ($c->get_req_user eq 'mayor' && ! $c->session('username')) {
		<ul>
			<li><label for="drawer-toggle">Join/Login</label>
		</ul>
		% }
	</nav>
	<footer>
		<%== $footer %>
	</footer>
</div>
<main>
	<%== $content %>
</main>
